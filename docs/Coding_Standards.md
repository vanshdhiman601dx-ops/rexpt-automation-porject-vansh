# Coding Standards

- Use clear test names that describe the expected behavior.
- Keep selectors in `locators`.
- Keep page interactions in `pages`.
- Keep assertions in specs or `validations`.
- Avoid hard-coded waits; prefer Playwright auto-waiting and explicit conditions.
- Store reusable input in `test-data`.
- Keep commits focused and small.
